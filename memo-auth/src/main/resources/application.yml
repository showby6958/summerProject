server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/memo_auth_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: wkdwlsgh9622
    password: wlsgh1236
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  redis:
      jwt:
        host: localhost
        port: 6379
        password: wlsgh1236

jwt:
  secret: dmVyeWltcG9ydGFudHNlY3JldGNvZGUnfbewdkyWbbhdvbVBVDW
  access-token-validity-seconds: 3600
  refresh-token-validity-seconds: 604800

  access-token-validity-ms: 3600000
  refresh-token-validity-ms: 1209600000

admin:
  # 개발 편의를 위한 기본값(placeholder)이며, 실제 값은 환경변수 ADMIN_EMAIL/ADMIN_PASSWORD로 주입할 것.
  # DB/Redis 비밀번호와 같은 값을 재사용하지 말 것 (AdminInitializer는 local/dev 프로필에서만 실행됨)
  email: ${ADMIN_EMAIL:admin@example.com}
  password: ${ADMIN_PASSWORD:changeme-local-only}

# 서비스 간 내부 호출 전용 API(/api/auth/users/**) 검증용 키 (memo-task 등 호출측과 동일해야 함)
internal-api:
  key: 4KxQ92zP1mR7vT0hN5cJ8dY3sW6eZaLb

# 로그인/로그아웃 쿠키의 Secure 속성. 운영(HTTPS) 배포 시 COOKIE_SECURE=true로 설정할 것.
# 로컬 http 환경에서 true로 두면 브라우저가 쿠키를 아예 전송하지 않아 인증이 깨진다.
cookie:
  secure: ${COOKIE_SECURE:false}
