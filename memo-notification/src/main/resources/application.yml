server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/memo_notification_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: wkdwlsgh9622
    password: wlsgh1236
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update


notification:
  stream:
    key: stream:task:events:0
    group: cg:notification
    consumer: notification-1
    poll-timeout-ms: 2000
    batch-size: 50

websocket:
  allowed-origins: "*"
  endpoint: /ws-notification
