server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/memo_task_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: wkdwlsgh9622
    password: wlsgh1236
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    chat:
      host: localhost
      port: 6380
      password: wlsgh1236

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: dmVyeWltcG9ydGFudHNlY3JldGNvZGUnfbewdkyWbbhdvbVBVDW

file:
  upload-dir: ./uploads

auth-service:
  base-url: http://localhost:8081

# memo-auth의 서비스 간 내부 호출 전용 API 검증용 키 (memo-auth와 동일해야 함)
internal-api:
  key: 4KxQ92zP1mR7vT0hN5cJ8dY3sW6eZaLb

outbox:
  relay:
    fixed-delay-ms: 200
    batch-size: 100
    max-attempts: 10
    base-backoff-ms: 1000
    max-backoff-ms: 300000
    jitter-ratio: 0.2
    processing-timeout-seconds: 60
    stream-key: stream:task:events:0
